name: uastodo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Map & Location
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  geolocator: ^11.0.1
  geocoding: ^2.1.1
  url_launcher: ^6.3.2
  
  # State Management & Storage
  provider: ^6.1.1
  shared_preferences: ^2.4.2
  sqflite: ^2.4.2
  path: ^1.9.1
  intl: ^0.18.1
  flutter_native_splash: ^2.3.7
  
  # HTTP untuk weather
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# HANYA SATU BAGIAN 'flutter:' ↓
flutter:
  uses-material-design: true
  
  assets:
    - assets/images/logos/logo.png
    - assets/images/logos/logo_small.png