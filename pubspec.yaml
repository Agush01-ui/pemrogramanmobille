name: uastodo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  geolocator: ^11.0.1
  geocoding: ^2.1.1
  provider: ^6.1.1
  shared_preferences: ^2.4.2
  sqflite: ^2.4.2
  path: ^1.9.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

# Hapus/comment flutter_typeahead untuk sementara