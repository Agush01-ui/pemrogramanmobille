name: todolistapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # DEPENDENSI TAMBAHAN AGAR HOME_SCREEN TIDAK ERROR
  cupertino_icons: ^1.0.6
  intl: ^0.18.1 # Wajib ditambahkan untuk DateFormat

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
